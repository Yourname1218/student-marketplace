# 🎨 前端部署完整步驟（GitHub Pages）

## ✅ 前置條件

- ✅ Render 後端已部署成功
- ✅ 資料庫已初始化成功
- ✅ 你的 Render 網址：`https://wa-bao-qu-campus.onrender.com`

---

## 📝 第一步：在本機修改檔案

### 步驟 1.1：建立環境變數檔案

在 `client` 資料夾中建立一個檔案叫 `.env.production`

**檔案路徑：** `C:\test\client\.env.production`

**檔案內容：**
```
VITE_API_URL=https://wa-bao-qu-campus.onrender.com
```

> ⚠️ **重要**：把 `wa-bao-qu-campus` 改成你實際的 Render 網址（如果不同）

---

### 步驟 1.2：更新 CORS 設定

打開 `server/server.js` 檔案，找到這一行（大約在第 18-24 行）：

```javascript
app.use(cors({
  origin: process.env.NODE_ENV === 'production' 
    ? [
        'https://your-github-username.github.io',
        'http://localhost:3000'
      ]
    : '*',
  credentials: true
}))
```

**修改成：**

```javascript
app.use(cors({
  origin: process.env.NODE_ENV === 'production' 
    ? [
        'https://Yourname1218.github.io',  // ← 改成你的 GitHub 使用者名稱
        'http://localhost:3000'
      ]
    : '*',
  credentials: true
}))
```

> ⚠️ **重要**：把 `Yourname1218` 改成你實際的 GitHub 使用者名稱

---

## 📤 第二步：上傳到 GitHub

在 CMD 中執行（在專案根目錄 `C:\test`）：

```bash
git add .
git commit -m "Configure frontend for GitHub Pages deployment"
git push
```

等待上傳完成。

---

## ⚙️ 第三步：在 GitHub 網站設定

### 步驟 3.1：設定 GitHub Secrets

1. 到你的 GitHub repository 頁面：
   ```
   https://github.com/Yourname1218/student-marketplace
   ```

2. 點擊上方的「**Settings**」標籤

3. 左側選單往下滾動，找到「**Secrets and variables**」

4. 點擊「**Actions**」

5. 點擊右上角的「**New repository secret**」按鈕

6. 填寫：
   - **Name**: `VITE_API_URL`
   - **Secret**: `https://wa-bao-qu-campus.onrender.com`（你的 Render 網址）
   
7. 點擊「**Add secret**」

---

### 步驟 3.2：啟用 GitHub Pages

1. 還在 Settings 頁面，左側選單找到「**Pages**」

2. 在「**Source**」區塊：
   - 選擇「**GitHub Actions**」

3. 設定完成！GitHub Pages 已經啟用

---

### 步驟 3.3：觸發自動部署

1. 點擊 repository 上方的「**Actions**」標籤

2. 左側應該會看到「**Deploy to GitHub Pages**」工作流程

3. 如果沒有自動執行，點擊「**Deploy to GitHub Pages**」

4. 點擊右側的「**Run workflow**」按鈕

5. 在下拉選單選擇「**main**」分支

6. 點擊綠色的「**Run workflow**」按鈕

7. 等待部署完成（約 2-3 分鐘）

---

## ✅ 第四步：查看部署結果

### 步驟 4.1：查看 GitHub Pages 網址

1. 回到 Settings → Pages

2. 你會在頁面頂部看到：
   ```
   Your site is live at https://Yourname1218.github.io/student-marketplace/
   ```

3. **記下這個網址**

---

### 步驟 4.2：測試網站

1. 打開瀏覽器

2. 訪問你的 GitHub Pages 網址：
   ```
   https://Yourname1218.github.io/student-marketplace/
   ```

3. 測試功能：
   - ✅ 看到首頁和商品列表
   - ✅ 測試登入（Email: `xiaoming@student.edu.tw`, 密碼: `123456`）
   - ✅ 瀏覽商品
   - ✅ 搜尋功能

---

## 🎉 完成！

你的網站已經完全部署完成了！

- **前端網址**：`https://Yourname1218.github.io/student-marketplace/`
- **後端網址**：`https://wa-bao-qu-campus.onrender.com`

---

## ⚠️ 常見問題

### Q1: Actions 顯示錯誤

**檢查：**
- ✅ 是否有設定 `VITE_API_URL` Secret？
- ✅ Secret 的值是否正確（包含 `https://`）？
- ✅ Render 網址是否正確？

### Q2: 網站顯示但無法登入

**檢查：**
- ✅ `server/server.js` 的 CORS 設定是否正確？
- ✅ GitHub 使用者名稱是否正確？
- ✅ Render 後端是否正常運行？

### Q3: 部署很慢

**正常現象：**
- GitHub Actions 第一次部署需要 2-3 分鐘
- 之後的更新會比較快（約 1-2 分鐘）

### Q4: 更新程式碼後如何重新部署？

**自動部署：**
- 只要執行 `git push`，GitHub Actions 會自動重新部署
- 不需要手動觸發

---

## 📋 檢查清單

部署前確認：
- [ ] 已建立 `client/.env.production` 檔案
- [ ] `.env.production` 中填入了正確的 Render 網址
- [ ] `server/server.js` 的 CORS 設定已更新
- [ ] 已執行 `git add .`、`git commit`、`git push`

GitHub 設定確認：
- [ ] 已設定 `VITE_API_URL` Secret
- [ ] GitHub Pages Source 設定為「GitHub Actions」
- [ ] 已觸發 Actions 部署

測試確認：
- [ ] 可以開啟網站
- [ ] 可以看到商品列表
- [ ] 可以成功登入
- [ ] 功能正常運作

---

**完成所有步驟後，你的網站就正式上線了！** 🎊

