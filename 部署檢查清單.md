# 快速部署檢查清單

## ✅ 部署前準備

- [ ] 已安裝 Git
- [ ] 已註冊 GitHub 帳號
- [ ] 已註冊 Render 帳號

---

## 📦 後端部署（Render）

### 1. 上傳到 GitHub
```bash
git init
git add .
git commit -m "Initial commit"
git remote add origin https://github.com/你的使用者名稱/student-marketplace.git
git branch -M main
git push -u origin main
```

### 2. 在 Render 建立服務
- [ ] 連接 GitHub repository
- [ ] Root Directory: `server`
- [ ] Build Command: `npm install`
- [ ] Start Command: `npm start`
- [ ] 選擇 Free plan
- [ ] 設定 `JWT_SECRET` 環境變數

### 3. 初始化資料庫
```bash
# 在 Render Shell 執行
cd server
npm run seed
```

### 4. 記下 Render 網址
```
https://your-app-name.onrender.com
```

---

## 🎨 前端部署（GitHub Pages）

### 1. 更新 API 網址
編輯 `client/.env.production`:
```
VITE_API_URL=https://your-app-name.onrender.com
```

### 2. 更新 CORS 設定
編輯 `server/server.js`，改成你的 GitHub 使用者名稱:
```javascript
'https://你的使用者名稱.github.io',
```

### 3. 提交變更
```bash
git add .
git commit -m "Update API URL"
git push
```

### 4. 設定 GitHub Pages
- [ ] Settings → Pages
- [ ] Source: GitHub Actions

### 5. 設定 Secret
- [ ] Settings → Secrets → Actions
- [ ] 新增: `VITE_API_URL` = 你的 Render 網址

### 6. 觸發部署
- [ ] Actions → Run workflow

---

## ✅ 測試

### 後端測試
開啟瀏覽器：
```
https://your-app-name.onrender.com/api
```
應該看到 JSON 訊息

### 前端測試  
開啟瀏覽器：
```
https://你的使用者名稱.github.io/student-marketplace/
```

### 登入測試
- Email: `xiaoming@student.edu.tw`
- 密碼: `123456`

---

## 🎉 完成！

你的網站已經上線了！

**前端**: `https://你的使用者名稱.github.io/student-marketplace/`

**後端**: `https://your-app-name.onrender.com`

---

## ⚠️ 重要提醒

1. Render 免費方案會休眠，第一次訪問需等待 30-60 秒
2. 如果修改程式碼，執行 `git push` 會自動重新部署
3. 記得將網址分享給朋友！

