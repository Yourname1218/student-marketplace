# 🚀 部署指南 - GitHub Pages + Render

這份指南教你如何將學生二手交易平台部署到網路上，讓任何人都可以使用！

---

## 📋 部署架構

```
前端（GitHub Pages）
    ↓ API 請求
後端（Render）
    ↓ 資料存取
資料庫（SQLite on Render）
```

- **前端**：部署在 GitHub Pages（完全免費）
- **後端 + 資料庫**：部署在 Render（免費方案）

---

## 🎯 準備工作

### 1. 註冊 GitHub 帳號
- 前往 https://github.com
- 如果還沒有帳號，註冊一個

### 2. 註冊 Render 帳號
- 前往 https://render.com
- 使用 GitHub 帳號登入（推薦）

---

## 📦 第一部分：部署後端到 Render

### 步驟 1：建立 GitHub Repository

1. 登入 GitHub
2. 點擊右上角「+」→「New repository」
3. 設定：
   - Repository name: `student-marketplace`
   - 選擇「Public」
   - 不要勾選任何選項
4. 點擊「Create repository」

### 步驟 2：上傳專案到 GitHub

在你的專案目錄（`C:\test`）執行：

```bash
# 初始化 Git
git init

# 加入所有檔案
git add .

# 提交
git commit -m "Initial commit"

# 連接到 GitHub（記得改成你的使用者名稱）
git remote add origin https://github.com/你的使用者名稱/student-marketplace.git

# 推送
git branch -M main
git push -u origin main
```

### 步驟 3：在 Render 建立後端服務

1. 登入 Render（https://render.com）
2. 點擊「New +」→「Web Service」
3. 選擇「Build and deploy from a Git repository」
4. 點擊「Connect」連接你的 GitHub
5. 找到 `student-marketplace` repository，點擊「Connect」
6. 設定：
   - **Name**: `student-marketplace-api`（或任何你喜歡的名字）
   - **Region**: Singapore（或選擇離台灣近的）
   - **Branch**: `main`
   - **Root Directory**: `server`
   - **Runtime**: `Node`
   - **Build Command**: `npm install`
   - **Start Command**: `npm start`
   - **Plan**: 選擇「Free」
7. 展開「Advanced」，新增環境變數：
   - **Key**: `JWT_SECRET`
   - **Value**: `your-secret-key-change-this-to-random-string`（改成任意隨機字串）
8. 點擊「Create Web Service」

### 步驟 4：等待部署完成

- Render 會開始建置和部署，大約需要 3-5 分鐘
- 完成後會顯示綠色的「Live」
- 你會得到一個網址，例如：`https://student-marketplace-api.onrender.com`
- **記下這個網址**，等下會用到！

### 步驟 5：初始化資料庫

1. 點擊右上角「Shell」
2. 在命令列輸入：
   ```bash
   cd server
   npm run seed
   ```
3. 看到「✅ 完成！」就表示資料建立成功

---

## 🎨 第二部分：部署前端到 GitHub Pages

### 步驟 1：更新 API 網址

1. 打開 `client/.env.production` 檔案
2. 將網址改成你的 Render 網址：
   ```
   VITE_API_URL=https://student-marketplace-api.onrender.com
   ```
   （記得改成你實際的 Render 網址）

### 步驟 2：更新 CORS 設定

1. 打開 `server/server.js`
2. 找到 CORS 設定的地方
3. 將 `your-github-username` 改成你的 GitHub 使用者名稱：
   ```javascript
   'https://你的使用者名稱.github.io',
   ```

### 步驟 3：提交變更

```bash
git add .
git commit -m "Update API URL for production"
git push
```

### 步驟 4：設定 GitHub Pages

1. 到你的 GitHub repository 頁面
2. 點擊「Settings」
3. 左側選單點擊「Pages」
4. Source 選擇「GitHub Actions」

### 步驟 5：設定 GitHub Secrets

1. 在 Settings 中，點擊左側「Secrets and variables」→「Actions」
2. 點擊「New repository secret」
3. 設定：
   - **Name**: `VITE_API_URL`
   - **Value**: `https://student-marketplace-api.onrender.com`（你的 Render 網址）
4. 點擊「Add secret」

### 步驟 6：觸發自動部署

GitHub Actions 會自動部署。如果沒有自動執行：

1. 到 repository 頁面
2. 點擊「Actions」標籤
3. 點擊左側「Deploy to GitHub Pages」
4. 點擊「Run workflow」→「Run workflow」

### 步驟 7：等待部署完成

- 部署大約需要 2-3 分鐘
- 完成後，到「Settings」→「Pages」查看網址
- 你的網站網址會是：`https://你的使用者名稱.github.io/student-marketplace/`

---

## ✅ 測試部署結果

### 1. 測試後端 API

在瀏覽器開啟：
```
https://student-marketplace-api.onrender.com/api
```

應該會看到：
```json
{"message": "學生二手交易平台 API"}
```

### 2. 測試前端網站

開啟你的 GitHub Pages 網址：
```
https://你的使用者名稱.github.io/student-marketplace/
```

### 3. 測試登入功能

使用測試帳號：
- Email: `xiaoming@student.edu.tw`
- 密碼: `123456`

如果可以成功登入並看到商品，恭喜你部署成功！🎉

---

## ⚠️ 重要注意事項

### Render 免費方案限制

1. **閒置休眠**：
   - 如果 15 分鐘沒有請求，伺服器會休眠
   - 下次訪問時需要等待 30-60 秒「喚醒」
   - 這是正常現象，不是錯誤

2. **每月限制**：
   - 免費方案每月有 750 小時的運行時間
   - 對個人專案來說綽綽有餘

### GitHub Pages 限制

1. **只能部署靜態檔案**
2. **Repository 大小上限 1GB**
3. **每月流量 100GB**（對個人專案足夠）

---

## 🔧 常見問題

### Q: 網站打開很慢或顯示錯誤？

**A**: Render 免費方案會休眠，第一次訪問需要等待 30-60 秒喚醒。重新整理頁面即可。

### Q: 登入後顯示 CORS 錯誤？

**A**: 檢查 `server/server.js` 的 CORS 設定是否正確填入你的 GitHub Pages 網址。

### Q: 商品資料不見了？

**A**: Render 免費方案重啟後資料會消失。重新執行 `npm run seed` 即可。

### Q: 想要更新網站內容？

**A**: 
```bash
# 修改程式碼後
git add .
git commit -m "Update feature"
git push
```
GitHub Actions 會自動重新部署。

### Q: 如何讓後端不休眠？

**A**: 
1. 升級到 Render 付費方案（$7/月）
2. 或使用 UptimeRobot 等服務定時 ping 你的 API

---

## 🎉 部署完成檢查清單

- ✅ Render 後端部署成功（顯示綠色 Live）
- ✅ 資料庫已初始化（執行過 seed）
- ✅ GitHub Pages 前端部署成功
- ✅ 可以開啟網站並看到商品
- ✅ 可以使用測試帳號登入
- ✅ 可以瀏覽、搜尋、收藏商品

---

## 🔗 你的網站連結

部署成功後，你可以分享：

- **網站**: `https://你的使用者名稱.github.io/student-marketplace/`
- **後端 API**: `https://student-marketplace-api.onrender.com`

---

## 📱 分享給朋友

把網站連結傳給朋友，他們就可以：
1. 註冊新帳號
2. 刊登商品
3. 瀏覽和購買二手商品

完全功能齊全，跟本地運行一模一樣！

---

**恭喜你完成部署！** 🎊

有任何問題隨時詢問！

